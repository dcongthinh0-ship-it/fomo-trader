# ADR-0001：交易执行与监控进程隔离

- 状态：Accepted
- 日期：2026-09-10

## 背景与决定

监控必须保持只读，而交易需要私钥、nonce 和可恢复交易状态。采用两个独立仓库、容器、SQLite 和 RPC 配置，只通过私有 Docker 网络上的带 HMAC 短 TTL 信号连接。

## 后果

故障域和密钥边界清晰；HTTP 至少一次投送需要交易端按 event_id 幂等。跨服务不共享事务，因此由 monitor outbox 与 trader 持久接收共同保证最终交接。运行成本增加一个轻量容器，但无需引入消息队列。
